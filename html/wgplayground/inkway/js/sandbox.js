/**
 * INKWAY — Sandbox Mode
 * =====================
 * Free-draw sandbox with unlimited ink.
 * Stub — full implementation coming.
 */

window.INKWAY = window.INKWAY || {};

INKWAY.Sandbox = (function () {
    'use strict';

    function init() {
        // Sandbox initialization
    }

    return { init };
})();
