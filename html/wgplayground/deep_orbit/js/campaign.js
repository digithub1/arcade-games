/**
 * Campaign presentation defaults.
 *
 * These are intentionally separate from FEEL/PARAMETERS.md: vehicle artwork/collision
 * scale can change without retuning the locked steering model.
 */
export const DEFAULT_CRAFT_SCALE = 1.45;
